// Найти максимальное значение среди всех элементов матрицы. Использовать вложенные do-while.
const matrix = [
    [3, 5, 9],
    [12, 1, 7],
    [4, 8, 2]
];

let n = matrix.length; //количество строк 
let m = matrix[0].length; //количество столбцов
let max = Number.NEGATIVE_INFINITY;
let i = 0;

do {
  do {
    
  }

} while ( < );